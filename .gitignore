# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/

# Database and uploads (never commit real shop data)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/backups/
uploads/*
!uploads/.gitkeep

# Belt-and-suspenders: scripts/export_real_data.py defaults to writing outside the
# repo, but never allow a *.export.json file to be committed even if one lands here.
*.export.json

# Secrets / env
.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
mcp_server.log
